window.__AMPLA_APP_CODE = (window.__AMPLA_APP_CODE || "") + "\")\n    ? parseBrazilianDate(task.archivedAt)\n    : parseDate(String(task.archivedAt).slice(0, 10));\n  const diff = Math.floor((new Date(TODAY.getFullYear(), TODAY.getMonth(), TODAY.getDate()) - archivedDate) / ONE_DAY);\n  return days === 0 ? diff === 0 : diff >= 0 && diff <= days;\n}\n\nfunction parseBrazilianDate(value) {\n  const [datePart] = String(value).split(\",\");\n  const [day, month, year] = datePart.split(\"/\").map(Number);\n  return new Date(year, month - 1, day);\n}\n\nfunction priorityClass(value) {\n  return `priority-${slug(value)}`;\n}\n\nfunction statusClass(value) {\n  return `status-${slug(value)}`;\n}\n\nfunction slug(value) {\n  return String(value)\n    .toLowerCase()\n    .normalize(\"NFD\")\n    .replace(/[\\u0300-\\u036f]/g, \"\")\n    .replace(/\\s+/g, \"-\");\n}\n\nfunction escapeHtml(value) {\n  return String(value ?? \"\")\n    .replaceAll(\"&\", \"&amp;\")\n    .replaceAll(\"<\", \"&lt;\")\n    .replaceAll(\">\", \"&gt;\")\n    .replaceAll('\"', \"&quot;\")\n    .replaceAll(\"'\", \"&#039;\");\n}\n\nfunction showNotice(text) {\n  $(\"noticeStrip\").textContent = text;\n  $(\"noticeStrip\").classList.add(\"is-visible\");\n}\n\nfunction hideNotice() {\n  $(\"noticeStrip\").classList.remove(\"is-visible\");\n}\n\nfunction flashNotice(text) {\n  showNotice(text);\n  window.clearTimeout(flashNotice.timer);\n  flashNotice.timer = window.setTimeout(() => {\n    if (state.tasks.length <= 200) hideNotice();\n  }, 4200);\n}\n\nfunction flashLoginMessage(text) {\n  $(\"loginHelp\").textContent = text;\n  window.clearTimeout(flashLoginMessage.timer);\n  flashLoginMessage.timer = window.setTimeout(renderLoginMessage, 4200);\n}\n";
